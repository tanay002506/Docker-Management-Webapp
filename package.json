{
  "name": "docker_project",
  "version": "1.0.0",
  "description": "manage docker containers with fewer commands",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
